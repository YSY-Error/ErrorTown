pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

// Dependency repositories live in build.gradle.kts so that one file describes the
// whole compile classpath.

rootProject.name = "ErrorTown"
